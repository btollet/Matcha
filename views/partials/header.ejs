<nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a href="/" class="navbar-brand">Matcha</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if (!login) { %>
        <ul class="navbar-nav mr-auto">
            <% if (page == 'accueil'){ %>
                <li class="nav-item active">
            <% } else { %>
                <li class="nav-item">
            <% } %>
            <a class="nav-link" href="/">Accueil</a>
            </li>
            <% if (page == 'register'){ %>
                <li class="nav-item active">
            <% } else { %>
                <li class="nav-item">
            <% } %>
            <a class="nav-link" href="/register">Inscription</a>
            </li>
        </ul>
    <% } else { %>
        <script>let my_login = '<%= login %>'</script>
        <script src="/socket.io/socket.io.js"></script>
        <script src="/script/navbar.js"></script>

        <ul class="navbar-nav mr-auto">
            <% if (page == 'wall'){ %>
                <li class="nav-item active">
            <% } else { %>
                <li class="nav-item">
            <% } %>
            <a class="nav-link" href="/">Accueil</a>
            </li>
            <% if (page == 'account'){ %>
                <li class="nav-item active">
            <% } else { %>
                <li class="nav-item">
            <% } %>
            <a class="nav-link" href="/account">Mon compte</a>
            </li>
        </ul>
        <ul class="navbar-nav my-2 my-lg-0">
            <li id='drop' class="nav-item dropdown">
                <button id='drop_but' type="button" class="btn btn-secondary dropdown-toggle" onclick='dropdown()' data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Notification <span id='notif_num'></span>
                </button>
                <div id='my_notif' class="dropdown-menu" style='overflow: auto; max-height: 400px'>
                    <script>check_notif()</script>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/deco">Deconnexion</a>
            </li>
        </ul>
    <% } %>
    </div>
</nav>
